version: 1
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
        - env | grep '^WORD_FINDER_' >> .env.production
    build:
      commands:
        - npm run test
        - npm run typecheck
        - npm run lint
        - npm run build
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
